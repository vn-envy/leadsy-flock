# Infra proof (27 Aug 2026)

Captured against the live Cloud Run revision after `scripts/e2e_infra.sh`.

- `e2e-campaign.json` — Peak Gym campaign with every pipeline receipt `ok`
- `runtime.json` — `GET /v1/infra` snapshot (Memory Bank id attached)
- `registry.json` — Agent Engine Memory Bank + Agent Registry handles
